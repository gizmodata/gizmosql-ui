allowBuilds:
  esbuild: true
  protobufjs: true
  sharp: true

# Exempt our own packages from pnpm's minimumReleaseAge supply-chain policy,
# so releases can consume a just-published @gizmodata/gizmosql-client.
minimumReleaseAgeExclude:
  - "@gizmodata/*"
